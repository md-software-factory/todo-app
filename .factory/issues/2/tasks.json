{
  "version": 1,
  "tasks": [
    {
      "id": "T-1",
      "title": "Replace hard-coded `return 3;` with `return a + b;` in the exported add(a, b) function",
      "dependsOn": [],
      "files": [
        "test.js"
      ],
      "acceptance": [
        "Importing test.js and calling add(2, 3) returns 5",
        "add(0, 0) returns 0 and add(-1, 4) returns 3",
        "No other exports or behavior in test.js change; diff is limited to test.js"
      ],
      "status": "done"
    }
  ]
}